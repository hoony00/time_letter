name: time_letter
description: "A new Flutter project."
gpublish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ^3.6.2

dependencies:
  flutter:
    sdk: flutter

  get: ^4.7.2
  hive: ^2.2.3
  hive_flutter: ^1.1.0

  firebase_core: ^3.12.1

  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^5.0.0

flutter:

  uses-material-design: true
  fonts:
    - family: NanumPen
      fonts:
        - asset: assets/fonts/NanumPenScript-Regular.ttf
